export const styles = theme => {
    const azanOffsetContainer = {
        display: "flex"
    };
    const azanOffset = {
        width: "20%",
        padding: "5px"
    };

    return ({
        azanOffsetContainer, azanOffset
    });
};