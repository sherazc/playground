import React from "react";
import { Text, View } from "react-native";

interface Props {
}

export const Settings: React.FC<Props> = () => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
}
