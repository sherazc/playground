# Techstack
- jdk 21 or the latest that can be deployed on aws ecs
- Spring boot
- thymeleaf
- aws ecs
- sqllite
- Amazon EFS (Elastic File System) volume for sqllite

# Plan
- The purpose of the project is to learn how to claude code to the best of its abilities.
- I have never used claude code
- I have bought the $20 subscription

# Requirement
- Create a todo list.
- Page 1: a form where the user enters a todo item; on submit it is persisted to the SQLite database.
- Page 2: the Spring controller loads the todos from the database into the http session, then passes the list to the view.
- The Thymeleaf UI iterates over the todo list passed in by the controller and displays it.

# AWS 
- Since this just a toy project, I want to keep the AWS bill as minimum as possible.

# Skills
- Expert java/spring developer
- Mid level AWS
- Zero level in Claude Code.
